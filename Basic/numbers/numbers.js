// regular number
const firstNumber = 23;
console.log(firstNumber, typeof(firstNumber));

// exponential number
const expNumber = 2e5;
console.log(expNumber, typeof(expNumber));

// hexadecimal number
const hexNumber = 0xFF;
console.log(hexNumber)


// floating number
const floatNumber = 0.1 + 0.2;
const fNumberMore = 0.2 + 0.5;
console.log(floatNumber, `another example: `,fNumberMore);

// number addition
const firstValue = 23;
const secondValue = "23";
const thirdValue = "Hello";

console.log(`addition: `, firstValue + secondValue, `subtraction: `, firstValue - secondValue, ` multiplication: `, firstValue * secondValue, `division: `, firstValue / secondValue);

console.log(secondValue + thirdValue)
console.log(secondValue - thirdValue)
console.log(typeof(secondValue));

let str = 'b' + 'a' + + 'a' + 'a';
console.log(str);